# Filesystem object example

Initialize this folder, register `src/graph.py`, place `[2, 3, 5]` in
`node/add_numbers/input/numbers.json`, and run `mwf runfrom add_numbers`.
The example uses only named filesystem objects for workflow input, output, and
downstream routing.
